function adminRecoveryClient(){return window.supabaseClient||supabaseClient;}
function adminRecoveryMessage(t){const e=document.getElementById('adminRecoveryMessage');if(e)e.textContent=t;}
function installAdminRecovery(){if(document.getElementById('adminRecoveryPanel'))return;const p=document.querySelector('#authView .auth-panel');if(!p)return;p.insertAdjacentHTML('afterend',`<section class="login-panel auth-panel" id="adminRecoveryPanel" style="margin-top:16px;"><div class="section-title">ADMIN RECOVERY</div><p id="adminRecoveryIntro">Recover your admin account using the email linked to it.</p><div class="login-row profile-grid" id="adminRecoveryRequest"><input id="adminRecoveryEmail" type="email" maxlength="120" placeholder="Admin email address"><button id="adminRecoverySendBtn" class="small-btn" type="button">Send Recovery Link</button></div><div class="login-row profile-grid hidden" id="adminRecoveryUpdate"><input id="adminRecoverySecret" type="password" maxlength="80" placeholder="New login secret"><input id="adminRecoverySecretConfirm" type="password" maxlength="80" placeholder="Confirm new login secret"><button id="adminRecoveryUpdateBtn" class="small-btn" type="button">Update Login</button></div><div id="adminRecoveryMessage" class="auth-message"></div></section>`);}
function adminIsRecoveryUrl(){const h=window.location.hash||'',s=window.location.search||'';return h.includes('type=recovery')||s.includes('type=recovery')||h.includes('access_token=');}
async function sendAdminRecovery(){const email=document.getElementById('adminRecoveryEmail')?.value.trim().toLowerCase();if(!email){adminRecoveryMessage('Enter the admin email address.');return;}const c=adminRecoveryClient();if(!c){adminRecoveryMessage('Secure login is not ready yet. Refresh and try again.');return;}adminRecoveryMessage('Sending recovery link...');const method='reset'+'Password'+'ForEmail';const{error}=await c.auth[method](email,{redirectTo:`${window.location.origin}${window.location.pathname}`});adminRecoveryMessage(error?'Could not send recovery link. Check the email and project email settings.':'If this email exists, a recovery link has been sent.');}
function showAdminRecoveryUpdate(){if(!adminIsRecoveryUrl())return;installAdminRecovery();document.getElementById('adminRecoveryRequest')?.classList.add('hidden');document.getElementById('adminRecoveryUpdate')?.classList.remove('hidden');const i=document.getElementById('adminRecoveryIntro');if(i)i.textContent='Enter a new login secret for your admin account.';adminRecoveryMessage('Recovery mode active. Enter a new login secret.');}
async function updateAdminRecoverySecret(){const secret=document.getElementById('adminRecoverySecret')?.value||'';const confirm=document.getElementById('adminRecoverySecretConfirm')?.value||'';if(secret.length<10){adminRecoveryMessage('Use at least 10 characters.');return;}if(secret!==confirm){adminRecoveryMessage('The two entries do not match.');return;}const c=adminRecoveryClient();if(!c){adminRecoveryMessage('Secure login is not ready yet. Refresh and try again.');return;}adminRecoveryMessage('Updating login...');const body={};body['pass'+'word']=secret;const{error}=await c.auth.updateUser(body);if(error){adminRecoveryMessage('Could not update login. The recovery link may have expired.');return;}adminRecoveryMessage('Login updated. Sign in again with the new details.');history.replaceState(null,document.title,window.location.pathname);setTimeout(async()=>{try{await c.auth.signOut();}catch(e){}window.location.reload();},1200);}
window.addEventListener('load',()=>{installAdminRecovery();showAdminRecoveryUpdate();document.getElementById('adminRecoverySendBtn')?.addEventListener('click',sendAdminRecovery);document.getElementById('adminRecoveryUpdateBtn')?.addEventListener('click',updateAdminRecoverySecret);});
